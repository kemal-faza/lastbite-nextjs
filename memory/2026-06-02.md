# 2026-06-02

## Session: M2 Product Catalog - Task 5 Image Upload

### Completed
- Task 5: Image Upload Infrastructure
- Installed multer, @aws-sdk/client-s3, @types/multer
- Added upload config (local + S3 dual provider) to config.ts
- Created uploadService.ts with uploadToLocal and uploadToS3
- Created multer middleware (memory storage, image-only filter)
- Created uploads route (POST /uploads, auth-protected)
- Registered route in app.ts with static file serving
- Wrote 3 tests (successful upload, 401 no auth, 400 no file) - all passing
- Added upload config to .env.example
- Created .gitignore for backend with uploads/ exclusion pattern

### Key Decisions
- Mounted express.static and uploadsRouter on same `/uploads` prefix - express.static only handles GET/HEAD, POST passes through to router
- Used multer memoryStorage (not diskStorage) since we write to disk ourselves in uploadService or upload to S3
- Dynamic import for @aws-sdk/client-s3 so S3 deps only load when needed
